import React from 'react';

const LoginPage = () => {
    return (
        <div className="container">
            <h2>Login Page</h2>
            <p>This is where the login form will be displayed.</p>
        </div>
    );
};

export default LoginPage;
